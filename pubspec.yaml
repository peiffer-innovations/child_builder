name: child_builder
description: A very simple builder that can be used by UI frameworks to customize / wrap internal widgets.
homepage: https://github.com/peiffer-innovations/child_builder

version: 1.1.0-nullsafety

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
